services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: asastamp
      POSTGRES_PASSWORD: asastamp
      POSTGRES_DB: asastamp_dev
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
